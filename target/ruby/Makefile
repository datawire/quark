include ../target.mk

EMIT_TARGET = --ruby

test: emit emit/test/ts.rb
	ruby -Iemit/test:emit/lib:rt/lib emit/test/ts.rb

emit/test/ts.rb:
	echo "# Generated by make" > $@
	for ts in $(basename $(notdir $(wildcard $(dir $@)ts_*.rb))); do echo "require_relative '$$ts'" >> $@; done
	cat $@
