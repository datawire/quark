# Setup file for package fancy_primitive

from setuptools import setup

setup(name="fancy_primitive",
      version="0.0.1",
      install_requires=["datawire-quark-core==0.3.1"],
      packages=['reflect', 'fancyprim', 'fancy_primitive_md'])
