# Setup file for package logme

from setuptools import setup

setup(name="logme",
      version="0.0.1",
      install_requires=["builtin==0.0.1"],
      py_modules=['logme'],
      packages=['logme_md'])
