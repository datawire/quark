# Setup file for package void_return

from setuptools import setup

setup(name="void_return",
      version="0.0.1",
      install_requires=["datawire-quark-core==0.4.2", "builtin==0.0.1"],
      py_modules=[],
      packages=['void_return_md'])
