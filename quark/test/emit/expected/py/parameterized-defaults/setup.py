# Setup file for package parameterized_defaults

from setuptools import setup

setup(name="parameterized_defaults",
      version="0.0.1",
      install_requires=["datawire-quark-core==0.3.1"],
      packages=['reflect', 'pkg', 'parameterized_defaults_md'])
