# Setup file for package list

from setuptools import setup

setup(name="list",
      version="0.0.1",
      install_requires=["builtin==0.0.1"],
      py_modules=['list'],
      packages=['list_md'])
