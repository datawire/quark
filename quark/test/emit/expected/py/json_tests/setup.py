# Setup file for package json_tests

from setuptools import setup

setup(name="json_tests",
      version="0.0.1",
      install_requires=["datawire-quark-core==0.4.1", "builtin==0.0.1"],
      packages=['json_tests_md'])
