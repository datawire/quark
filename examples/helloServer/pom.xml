<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>hello_server</groupId>
  <artifactId>hello_server</artifactId>
  <version>0.0</version>
  <name>hello_server</name>
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.3</version>
        <configuration>
          <source>1.7</source>
          <target>1.7</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>2.10.3</version>
        <configuration>
          <excludePackageNames>io.datawire:*.Functions</excludePackageNames>
        </configuration>
      </plugin>
      <plugin>
        <groupId>io.datawire.quark.build</groupId>
        <artifactId>quark-maven-plugin</artifactId>
        <version>0.2.0-SNAPSHOT</version>
        <executions>
          <execution>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
        <quarkCommand>${quarkCommand}</quarkCommand>
        </configuration>
      </plugin>
    </plugins>
  </build>
  <properties>
    <!-- The purpose of the indirection for quark command is to allow
         overriding it in a settings.xml profile and activating that
         profile inside eclipse maven settings for the project
         This is only necessary if the quark command is installed in a
         non-standard location, for example inside a virtualenv
    -->
  <quarkCommand>quark</quarkCommand>
  </properties>
  <dependencies>
    <dependency>
      <groupId>io.datawire.quark</groupId>
      <artifactId>quark-netty</artifactId>
      <version>0.2.0-SNAPSHOT</version>
      <scope>compile</scope>
    </dependency>
  </dependencies>
</project>
