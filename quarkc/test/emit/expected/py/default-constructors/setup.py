# Setup file for package default_constructors

from setuptools import setup

setup(name="default_constructors",
      version="0.0.1",
      install_requires=["quark==0.0.1"],
      py_modules=['default_constructors'],
      packages=['default_constructors', 'default_constructors_md'])
