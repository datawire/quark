# Setup file for package fancy_primitive

from setuptools import setup

setup(name="fancy_primitive",
      version="0.0.1",
      install_requires=["builtin==0.0.1"],
      py_modules=['fancy_primitive'],
      packages=['fancyprim', 'fancy_primitive', 'fancy_primitive_md'])
