# Setup file for package macro_native

from setuptools import setup

setup(name="macro_native",
      version="0.0.1",
      install_requires=["quark==0.0.1"],
      py_modules=[],
      packages=['macro_native', 'macro_native_md'])
