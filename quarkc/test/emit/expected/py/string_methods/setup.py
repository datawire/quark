# Setup file for package string_methods

from setuptools import setup

setup(name="string_methods",
      version="0.0.1",
      install_requires=["quark==0.0.1"],
      py_modules=['string_methods'],
      packages=['string_methods', 'string_methods_md'])
