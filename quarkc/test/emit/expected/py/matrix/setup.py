# Setup file for package matrix

from setuptools import setup

setup(name="matrix",
      version="0.0.1",
      install_requires=["builtin==0.0.1"],
      py_modules=['matrix'],
      packages=['matrix', 'matrix_md'])
