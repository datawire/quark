# Setup file for package modulo

from setuptools import setup

setup(name="modulo",
      version="0.0.1",
      install_requires=["builtin==0.0.1"],
      py_modules=['modulo'],
      packages=['modulo_md'])
