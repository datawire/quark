# Setup file for package implicit_self

from setuptools import setup

setup(name="implicit_self",
      version="0.0.1",
      install_requires=["quark==0.0.1"],
      py_modules=[],
      packages=['implicit_self', 'implicit_self_md'])
